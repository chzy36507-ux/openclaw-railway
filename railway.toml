[build]
  # 构建命令（如 npm install）
  build = "npm install"  
  # 构建器：nixpacks（自动检测环境）或 dockerfile（需指定 Dockerfile 路径）
  builder = "nixpacks"  

[scripts]
  # 启动命令（如 node index.js）
  start = "node index.js"  

# 环境变量（可选，如数据库连接串）
# [env]
#   DATABASE_URL = "your_db_url"
